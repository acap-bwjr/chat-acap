# Módulo de Prospecção (CRM/Pipeline) — opcional.
# Sem estas variáveis, o módulo fica desconectado (nenhuma leitura/escrita real).
# Use um projeto Supabase separado do de produção para não misturar dados de dev.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Login com Google (Google Cloud Console > APIs e serviços > Credenciais)
VITE_GOOGLE_CLIENT_ID=
